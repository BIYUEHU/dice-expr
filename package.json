{
  "name": "dice-expr",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "node build/exec/Main.js",
    "dev": "python scripts/watch.py --root src --ext .idr --cmd \"npm run dev:only\"",
    "dev:only": "npm run build && npm run start",
    "build": "idris2 --cg node --build"
  }
}